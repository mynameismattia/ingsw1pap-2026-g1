// I 4 esiti possibili di una mano: WIN, LOSE, PUSH, BLACKJACK naturale.
// Pilota colori, payout e messaggi a fine round.

package ch.supsi.dti.backend.model;

public enum HandOutcome {
    WIN,
    LOSE,
    PUSH,
    BLACKJACK
}
