// Enum dei 4 semi del mazzo (cuori, quadri, fiori, picche). Ogni Card ne ha esattamente uno.
// Niente logica: solo i 4 valori. È l'atomo più basso del dominio.

package ch.supsi.dti.backend.model;

public enum Suit{
    HEARTS,
    DIAMONDS,
    CLUBS,
    SPADES
}
